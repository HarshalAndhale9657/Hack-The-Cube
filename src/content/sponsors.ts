import type { Sponsor } from "@/content/schemas";

// Sponsors will be announced closer to the event.
// The SponsorGrid component now renders 3 placeholder boxes independently.
export const sponsors: Sponsor[] = [];
